# RAG-Onboarding-Assistant-FastAPI
RAG-based onboarding assistant built with FastAPI and ChromaDB. Implements semantic search using a persistent vector database and generates context-aware AI responses using an LLM API.
